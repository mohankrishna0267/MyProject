package com.agriserve.enums;

/** Lifecycle status shared by sessions, programs, workshops, reports, content. */
public enum Status {
    DRAFT,
    SCHEDULED,
    ACTIVE,
    COMPLETED,
    CANCELLED,
    ARCHIVED
}
